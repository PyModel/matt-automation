{
  "name": "matt-automations",
  "private": true,
  "type": "module",
  "description": "Autonomous drivers for Matt Pocock's skills",
  "engines": { "node": ">=20" },
  "scripts": {
    "check": "node scripts/matt.mjs check",
    "test": "node --test scripts/*.test.mjs && node to-orc/scripts/selftest.mjs && python3 matt-automation-audit/audit_regressions.py --repo . --output \"${TMPDIR:-/tmp}/matt-audit-results.json\""
  }
}
