# Close: stages 8 to 10

Each stage is done per PIPELINE.md § Completion criteria per stage.

8. **Final review, fresh context.** `node ROOT/scripts/goal.mjs registry <slug> reviewing`. The orchestrator holds every assumption that shaped the code, so it does not review. Dispatch one **review subagent** whose brief is only: `review_base` (BOOTSTRAP.md § 0c), the run branch, the spec path, `CONTEXT.md`, the ledger path, and "load `code-review` by path (`node ROOT/scripts/matt.mjs resolve code-review`) and run it against `<review_base>` with `<spec path>`, briefing its sub-agents per PITFALLS.md (code-review rows); then `defensive-design` in Review mode over `review_base...HEAD` for every module at Tier 2 or 3; load neither skill again; return the two reports verbatim", then LEDGER.md § Subagent contract. Hand every cited finding (a standards rule, a smell plus hunk, a spec line, a defensive-design finding with evidence) to one **fix subagent** (implement-spec step 7) in the run worktree; log uncited leads. One review pass, one fix pass; then an independent, targeted re-verification of each fixed finding against the resulting snapshot, and the suite once more. Write the verdict to `runs/<slug>/final-verdict.json`.
9. **Hand back.** Local tracker (default): nothing leaves the machine; the run branch and worktree stay for the user to merge; the run branch carries only code plus the glossary commit. The report names the branch, `goal/bootstrap` to fast-forward first if stage 0b created it, the glossary delta, the quarantine list, and the dirty-checkout warning if any. GitHub tracker: push the run branch and mark the draft PR ready only under the push authority rule (SKILL.md § Rules, Autonomous); tracker configuration alone is never push authority. Remove every ticket worktree (`git worktree prune` after).
10. **Retro.** Load retro and run it over this run's ledger, events, and status files in the control plane (not harness session logs), writing candidates by severity (navigation pointers, automated checks, coding-standards rules for the reviewer, no-ops in steering files, tool economy, information access) to `runs/<slug>/retro.md`. Apply nothing. Include budget used versus cap. Then rewrite NOW to `stage: done`, tick `10`, `node ROOT/scripts/goal.mjs registry <slug> done`, commit the control plane, and write the final report (SKILL.md § Final report).
