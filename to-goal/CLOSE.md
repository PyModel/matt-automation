# Close: stages 8 to 10

8. **Final review, fresh context.** Registry `status: reviewing`. The orchestrator holds every assumption that shaped the code, so it does not review. Dispatch one **review subagent** whose brief is only: the branch point, the run branch, the spec path, `CONTEXT.md`, the ledger path, and "call the skill tool with `code-review` against `<branch-point>` with `<spec path>`, then `defensive-design` in Review mode over the same diff for every module at Tier 2 or 3; do not invoke either skill again and spawn no other agents; return the two reports verbatim". Hand every cited finding (a standards rule, a smell plus hunk, a spec line, a defensive-design finding with evidence) to one **fix subagent** (implement-spec step 7) in the run worktree; log uncited leads. One review pass; one fix pass; then the suite once more. Done when cited findings are fixed and committed and the suite is no worse than baseline.
9. **Hand back.** Local tracker (default): nothing leaves the machine; the run branch and worktree stay for the user to merge; the run branch carries only code plus the glossary commit, no ledger or tracker files (those are on `goal/control`). The report names the branch, `goal/bootstrap` to fast-forward first if stage 0b created it, the glossary delta, the quarantine list, and the dirty-checkout warning if any. GitHub tracker only: push the run branch and mark the draft PR ready. Remove every ticket worktree (`git worktree prune` after). Done when `git worktree list` shows only the user's checkout and the run worktree.
10. **Retro.** Load retro and run it over this run's ledger, events, and status files in the control plane (not harness session logs), writing candidates by severity (navigation pointers, automated checks, coding-standards rules for the reviewer, no-ops in steering files, tool economy, information access) to `.worktrees/control/runs/<slug>/retro.md`. Apply nothing. Include budget used versus cap. Then rewrite NOW to `stage: done`, set the registry `status: done`, commit the control plane, and write the final report.
